# Mac download

There is no `llm-observe.dmg` yet.

The landing page previously linked here; without a real file, the dev server
returned an HTML error page, which macOS reported as a corrupted .dmg.

When you have a built Mac app, place it here as:

  llm-observe.dmg

Then re-enable the download button in `src/pages/Landing.tsx`.
